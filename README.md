# 🌍 Xiao-World - ระบบเผยแพร่เนื้อหาหลายแพลตฟอร์ม

> **ดึงเนื้อหาจากเสี้ยวหงชู (小红书) แปลภาษาด้วย AI แล้วเผยแพร่ไปทั่วโลก!**
>
> รองรับ Twitter, Facebook, TikTok, YouTube - ใช้งานผ่าน Claude Desktop หรือ AI อื่นๆ

[![Made with Go](https://img.shields.io/badge/Made%20with-Go-00ADD8?style=flat-square&logo=go)](https://golang.org)
[![License](https://img.shields.io/badge/License-MIT-green.svg?style=flat-square)](LICENSE)
[![MCP](https://img.shields.io/badge/MCP-Protocol-blue?style=flat-square)](https://modelcontextprotocol.io)

---

## 📖 สารบัญ

- [โปรแกรมนี้คืออะไร?](#-โปรแกรมนี้คืออะไร)
- [ทำอะไรได้บ้าง?](#-ทำอะไรได้บ้าง)
- [ติดตั้งอย่างไร](#-ติดตั้งอย่างไร)
  - [Windows](#-windows)
  - [macOS](#-macos)
  - [Linux](#-linux)
- [ตั้งค่า API Keys](#-ตั้งค่า-api-keys)
- [เริ่มใช้งาน](#-เริ่มใช้งาน)
- [ตัวอย่างการใช้งาน](#-ตัวอย่างการใช้งาน)
- [แก้ปัญหา](#-แก้ปัญหา)
- [FAQ](#-faq)

---

## 🤔 โปรแกรมนี้คืออะไร?

**Xiao-World** เป็น MCP Server ที่ช่วยให้คุณ:

1. 📱 **ดึงเนื้อหาจากเสี้ยวหงชู** (小红书/RedNote)
2. 🤖 **แปลภาษาอัตโนมัติด้วย AI** (ChatGPT, Claude, Gemini)
3. 🌍 **เผยแพร่ไปหลายแพลตฟอร์ม** (Twitter, Facebook, TikTok, YouTube)

### เหมาะกับใคร?

- ✅ Content Creator ที่ต้องการขยายผู้ชมต่างประเทศ
- ✅ นักการตลาดที่ทำ Multi-channel Marketing
- ✅ ธุรกิจที่ต้องการประหยัดเวลาการโพสต์
- ✅ ทุกคนที่ต้องการแชร์เนื้อหาไปหลายช่องทางพร้อมกัน

---

## ✨ ทำอะไรได้บ้าง?

### 🎯 ฟีเจอร์หลัก

| ฟีเจอร์ | คำอธิบาย |
|---------|----------|
| 🤖 **แปลภาษา AI** | ChatGPT, Claude, Gemini |
| 🌍 **โพสต์หลายที่พร้อมกัน** | 4 แพลตฟอร์มพร้อมกัน |
| 🔄 **ปรับเนื้อหาอัตโนมัติ** | ตัดข้อความให้เหมาะกับแต่ละแพลตฟอร์ม |
| 📝 **รองรับหลายรูปแบบ** | ข้อความ, รูปภาพ, วิดีโอ |

### 📱 แพลตฟอร์มที่รองรับ

| แพลตฟอร์ม | ข้อความ | รูปภาพ | วิดีโอ | ข้อจำกัด |
|-----------|---------|--------|--------|----------|
| 🐦 Twitter | ✅ | ✅ (4 รูป) | ❌ | 280 ตัวอักษร |
| 📘 Facebook | ✅ | ✅ | ✅ | ไม่จำกัด |
| 🎵 TikTok | ❌ | ❌ | ✅ | วิดีโอเท่านั้น |
| 🎬 YouTube | ❌ | ❌ | ✅ | วิดีโอเท่านั้น |

---

## 🚀 ติดตั้งอย่างไร

เลือกตาม OS ของคุณ:
- [Windows](#-windows)
- [macOS](#-macos)
- [Linux](#-linux)

---

### 🪟 Windows

#### ขั้นตอนที่ 1: ติดตั้ง Go

1. **ดาวน์โหลด Go:**
   - เปิด https://go.dev/dl/
   - ดาวน์โหลด **`go1.23.x.windows-amd64.msi`** (เวอร์ชันล่าสุด)

2. **ติดตั้ง:**
   - Double-click ไฟล์ `.msi`
   - กด **Next** → **Next** → **Install**
   - รอจนเสร็จ → กด **Finish**

3. **ตรวจสอบ:**
   - กด **Win + R**
   - พิมพ์ `cmd` แล้วกด Enter
   - พิมพ์:
     ```cmd
     go version
     ```
   - ควรเห็น: `go version go1.23.x windows/amd64`

#### ขั้นตอนที่ 2: ดาวน์โหลด xiao-world

```cmd
# ไปที่ Desktop
cd %USERPROFILE%\Desktop

# Clone repository
git clone https://github.com/huge8888/xiao-world.git

# เข้าโฟลเดอร์
cd xiao-world
```

**ถ้าไม่มี git:**
1. ดาวน์โหลดจาก: https://github.com/huge8888/xiao-world/archive/refs/heads/main.zip
2. แตกไฟล์ที่ Desktop
3. เปลี่ยนชื่อโฟลเดอร์เป็น `xiao-world`

#### ขั้นตอนที่ 3: Build โปรแกรม

```cmd
# Build
go build -o xiao-world.exe .

# ถ้าสำเร็จจะเห็นไฟล์ xiao-world.exe
dir xiao-world.exe
```

#### ขั้นตอนที่ 4: สร้างไฟล์ .env

```cmd
# Copy ตัวอย่าง
copy .env.example .env

# เปิดแก้ไข
notepad .env
```

**ไปต่อที่:** [ตั้งค่า API Keys](#-ตั้งค่า-api-keys)

---

### 🍎 macOS

#### ขั้นตอนที่ 1: ติดตั้ง Homebrew (ถ้ายังไม่มี)

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

#### ขั้นตอนที่ 2: ติดตั้ง Go

```bash
# ติดตั้ง Go
brew install go

# ตรวจสอบ
go version
```

**ถ้าไม่ใช้ Homebrew:**
1. ดาวน์โหลดจาก: https://go.dev/dl/
2. เลือก **`go1.23.x.darwin-amd64.pkg`** (Intel) หรือ **`darwin-arm64.pkg`** (Apple Silicon)
3. Double-click ติดตั้ง

#### ขั้นตอนที่ 3: Clone และ Build

```bash
# ไปที่ Desktop
cd ~/Desktop

# Clone
git clone https://github.com/huge8888/xiao-world.git
cd xiao-world

# Build
go build -o xiao-world .

# ให้สิทธิ์รัน
chmod +x xiao-world
```

#### ขั้นตอนที่ 4: สร้างไฟล์ .env

```bash
# Copy ตัวอย่าง
cp .env.example .env

# เปิดแก้ไข (เลือกอันใดอันหนึ่ง)
open -e .env        # TextEdit
nano .env           # Terminal editor
code .env           # VS Code (ถ้ามี)
```

**ไปต่อที่:** [ตั้งค่า API Keys](#-ตั้งค่า-api-keys)

---

### 🐧 Linux

#### ขั้นตอนที่ 1: ติดตั้ง Go

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install golang-go -y
```

**Fedora/RHEL:**
```bash
sudo dnf install golang -y
```

**Arch Linux:**
```bash
sudo pacman -S go
```

**ตรวจสอบ:**
```bash
go version
```

#### ขั้นตอนที่ 2: Clone และ Build

```bash
# ไปที่ Home
cd ~

# Clone
git clone https://github.com/huge8888/xiao-world.git
cd xiao-world

# Build
go build -o xiao-world .

# ให้สิทธิ์รัน
chmod +x xiao-world
```

#### ขั้นตอนที่ 3: สร้างไฟล์ .env

```bash
# Copy ตัวอย่าง
cp .env.example .env

# เปิดแก้ไข
nano .env
# หรือ
vim .env
```

**ไปต่อที่:** [ตั้งค่า API Keys](#-ตั้งค่า-api-keys)

---

## ⚙️ ตั้งค่า API Keys

เปิดไฟล์ `.env` แล้วตั้งค่าตามนี้:

### 1. เลือก AI Translator (เลือก 1 อัน)

#### 🥇 ChatGPT (OpenAI) - แนะนำ

**ทำไม?** ถูก ($0.15/1000 คำ), ได้ดี, ฟรี $5 เมื่อสมัครใหม่

**วิธีขอ API Key:**

1. ไปที่: https://platform.openai.com/signup
2. สมัครบัญชี
3. ไปที่: https://platform.openai.com/api-keys
4. คลิก **"Create new secret key"**
5. ตั้งชื่อ: `xiao-world`
6. คัดลอก API Key (ขึ้นต้นด้วย `sk-proj-`)

**ใส่ใน .env:**
```bash
AI_TRANSLATOR_PROVIDER=openai
AI_TRANSLATOR_API_KEY=sk-proj-xxxxxxxxxxxxx
AI_TRANSLATOR_MODEL=gpt-4o-mini
```

---

#### 🥈 Claude (Anthropic)

**ทำไม?** แปลเป็นธรรมชาติมาก

**วิธีขอ API Key:**

1. ไปที่: https://console.anthropic.com/
2. สมัครบัญชี
3. Settings → API Keys → **Create Key**
4. คัดลอก (ขึ้นต้นด้วย `sk-ant-`)

**ใส่ใน .env:**
```bash
AI_TRANSLATOR_PROVIDER=anthropic
AI_TRANSLATOR_API_KEY=sk-ant-xxxxxxxxxxxxx
AI_TRANSLATOR_MODEL=claude-3-haiku-20240307
```

**ราคา:** ~$0.25/1000 คำ

---

#### 🥉 Gemini (Google) - ฟรี!

**ทำไม?** ฟรี! เหมาะทดลอง

**วิธีขอ API Key:**

1. ไปที่: https://makersuite.google.com/app/apikey
2. Login ด้วย Google
3. **Create API Key**
4. คัดลอก (ขึ้นต้นด้วย `AIzaSy`)

**ใส่ใน .env:**
```bash
AI_TRANSLATOR_PROVIDER=google
AI_TRANSLATOR_API_KEY=AIzaSyxxxxxxxxxxxxx
AI_TRANSLATOR_MODEL=gemini-1.5-flash
```

---

### 2. ตั้งค่าแพลตฟอร์ม

เปิดเฉพาะที่ต้องการใช้:

#### 🐦 Twitter

**วิธีขอ API Key:**

1. ไปที่: https://developer.twitter.com/en/portal/dashboard
2. สมัคร Developer Account
3. สร้าง App ใหม่
4. Keys and tokens → **Generate Bearer Token**
5. คัดลอก Token

**ใส่ใน .env:**
```bash
TWITTER_ENABLED=true
TWITTER_BEARER_TOKEN=AAAAAAAAxxxxxxxxxxxxxxxxx
```

---

#### 📘 Facebook

**วิธีขอ API Key:**

1. ไปที่: https://developers.facebook.com/
2. สร้าง App → เลือก **"Business"**
3. Add Product: **Facebook Login** + **Pages**
4. Graph API Explorer:
   - เลือก Page
   - ขอ permissions: `pages_manage_posts`, `pages_read_engagement`
   - Generate **Page Access Token**
5. หา Page ID: ไปที่เพจ → About → Page ID

**ใส่ใน .env:**
```bash
FACEBOOK_ENABLED=true
FACEBOOK_ACCESS_TOKEN=EAAxxxxxxxxxxxxx
FACEBOOK_PAGE_ID=123456789
```

---

#### 🎵 TikTok

⚠️ **หมายเหตุ:** ต้องรออนุมัติ 1-2 สัปดาห์

1. ไปที่: https://developers.tiktok.com/
2. สมัคร Developer Account
3. สร้าง App → **Content Posting API**
4. รอ TikTok อนุมัติ
5. ขอ Access Token

**ใส่ใน .env:**
```bash
TIKTOK_ENABLED=true
TIKTOK_ACCESS_TOKEN=xxxxxxxxxxxxx
```

---

#### 🎬 YouTube

**วิธีขอ API Key:**

1. ไปที่: https://console.cloud.google.com/
2. สร้าง Project ใหม่
3. เปิด **YouTube Data API v3**
4. สร้าง OAuth 2.0 Credentials
5. Download client secrets
6. รัน OAuth flow เพื่อขอ tokens

**ใส่ใน .env:**
```bash
YOUTUBE_ENABLED=true
YOUTUBE_CLIENT_ID=xxxxx.apps.googleusercontent.com
YOUTUBE_CLIENT_SECRET=xxxxxxxxxxxxx
YOUTUBE_ACCESS_TOKEN=ya29.xxxxxxxxxxxxx
YOUTUBE_REFRESH_TOKEN=1//xxxxxxxxxxxxx
```

---

### 3. บันทึกไฟล์

- Windows: กด **Ctrl+S**
- Mac: กด **Cmd+S**
- Linux: กด **Ctrl+O** แล้ว **Enter**

---

## 🎮 เริ่มใช้งาน

### วิธีที่ 1: ใช้ผ่าน Claude Desktop (แนะนำ)

#### ขั้นตอนที่ 1: รัน xiao-world

**Windows:**
```cmd
cd %USERPROFILE%\Desktop\xiao-world
xiao-world.exe
```

**Mac/Linux:**
```bash
cd ~/Desktop/xiao-world  # หรือ path ที่คุณเก็บไว้
./xiao-world
```

**ควรเห็น:**
```
INFO ✅ ใช้ AI Translator: openai (model: gpt-4o-mini)
INFO ✅ Twitter publisher enabled
INFO ✅ Facebook publisher enabled
INFO 🚀 Server listening on :18060
```

#### ขั้นตอนที่ 2: ตั้งค่า Claude Desktop

1. เปิด **Claude Desktop**
2. ไปที่ **Settings** (⚙️) → **Developer** → **Edit Config**
3. จะเปิดไฟล์ `claude_desktop_config.json`

**ใส่โค้ดนี้:**

**Windows:**
```json
{
  "mcpServers": {
    "xiao-world": {
      "command": "C:\\Users\\YourName\\Desktop\\xiao-world\\xiao-world.exe",
      "args": []
    }
  }
}
```

**Mac:**
```json
{
  "mcpServers": {
    "xiao-world": {
      "command": "/Users/yourname/Desktop/xiao-world/xiao-world",
      "args": []
    }
  }
}
```

**Linux:**
```json
{
  "mcpServers": {
    "xiao-world": {
      "command": "/home/yourname/xiao-world/xiao-world",
      "args": []
    }
  }
}
```

⚠️ **สำคัญ:** เปลี่ยน `YourName` และ `yourname` เป็นชื่อจริงของคุณ!

4. บันทึกไฟล์
5. **Restart Claude Desktop** (ปิดแล้วเปิดใหม่)

#### ขั้นตอนที่ 3: ทดสอบ

พิมพ์ใน Claude Desktop:
```
List available tools
```

ควรเห็น tools ของ xiao-world เช่น:
- `get_feed_detail`
- `search_feeds`
- `publish_to_twitter`
- `publish_to_facebook`
- etc.

---

### วิธีที่ 2: ใช้ผ่าน curl (สำหรับ Developer)

#### รัน xiao-world (ถ้ายังไม่ได้รัน)

```bash
./xiao-world
```

#### ตัวอย่างคำสั่ง

**1. ค้นหาโพสต์จากเสี้ยวหงชู:**
```bash
curl -X POST http://localhost:18060/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "search_feeds",
      "arguments": {
        "keyword": "เที่ยวญี่ปุ่น",
        "sort_type": "popularity"
      }
    },
    "id": 1
  }'
```

**2. ดึงรายละเอียดโพสต์:**
```bash
curl -X POST http://localhost:18060/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "get_feed_detail",
      "arguments": {
        "feed_id": "67xxxxxxxxxxxxx",
        "xsec_token": "XYZ..."
      }
    },
    "id": 2
  }'
```

**3. โพสต์ไป Twitter:**
```bash
curl -X POST http://localhost:18060/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "publish_to_twitter",
      "arguments": {
        "feed_id": "67xxxxxxxxxxxxx",
        "xsec_token": "XYZ..."
      }
    },
    "id": 3
  }'
```

---

## 🎨 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: ค้นหาและโพสต์ไป Twitter

**ใน Claude Desktop พิมพ์:**
```
ช่วยค้นหาโพสต์เกี่ยวกับ "เที่ยวญี่ปุ่น"
แล้วเลือกโพสต์ยอดนิยมโพสต์ไป Twitter ด้วย
```

**Claude จะ:**
1. ✅ ค้นหาโพสต์จากเสี้ยวหงชู
2. ✅ แสดงรายการให้เลือก
3. ✅ ดึงข้อมูลโพสต์ที่เลือก
4. ✅ แปลเป็นภาษาอังกฤษ
5. ✅ โพสต์ไป Twitter
6. ✅ ส่งลิงก์โพสต์กลับมา

**ผลลัพธ์:**
```
✅ โพสต์สำเร็จ!

📱 Twitter: https://twitter.com/yourusername/status/1234567890
📊 Engagement: 15 likes, 3 retweets
```

---

### ตัวอย่างที่ 2: โพสต์ไปทุกแพลตฟอร์ม

**ใน Claude Desktop:**
```
เอาโพสต์นี้ไปโพสต์ทุกแพลตฟอร์มเลย
feed_id: 67xxxxxxxxxxxxx
```

**ผลลัพธ์:**
```
📊 ผลการเผยแพร่:

✅ Twitter: สำเร็จ
   🔗 https://twitter.com/xxx/status/123

✅ Facebook: สำเร็จ
   🔗 https://facebook.com/123

❌ TikTok: ข้าม (ไม่มีวิดีโอ)

❌ YouTube: ข้าม (ไม่ได้เปิดใช้งาน)

📈 สรุป: 2 สำเร็จ, 0 ล้มเหลว, 2 ข้าม
```

---

### ตัวอย่างที่ 3: แปลภาษาเป็นไทย

**ใน Claude Desktop:**
```
ดึงโพสต์นี้มาแล้วแปลเป็นภาษาไทย
feed_id: 67xxxxxxxxxxxxx
```

**Claude จะแปลให้อัตโนมัติ!**

---

## 🐛 แก้ปัญหา

### ❌ โปรแกรมรันไม่ได้

**อาการ:** Double-click แล้วปิดทันที

**วิธีแก้:**
1. **อย่า double-click!**
2. เปิด Terminal/Command Prompt
3. `cd` เข้าไปในโฟลเดอร์
4. รันด้วยคำสั่ง:
   ```bash
   # Windows
   .\xiao-world.exe

   # Mac/Linux
   ./xiao-world
   ```
5. อ่าน error message

**Error ที่พบบ่อย:**

| Error | สาเหตุ | วิธีแก้ |
|-------|--------|---------|
| `cannot find .env` | ไม่มีไฟล์ .env | สร้างจาก `.env.example` |
| `invalid API key` | API Key ผิด | เช็คไฟล์ .env |
| `port 18060 in use` | Port ถูกใช้แล้ว | ปิดโปรแกรมเดิมหรือเปลี่ยน port |

---

### ❌ ไม่เห็น MCP tools ใน Claude Desktop

**อาการ:** Claude บอกว่าไม่รู้จัก xiao-world

**วิธีแก้:**
1. ✅ เช็คว่า xiao-world กำลังรันอยู่
2. ✅ เช็ค config path ถูกต้อง
3. ✅ **Restart Claude Desktop**
4. ✅ พิมพ์: `list available tools`

**Config path:**
- Mac: `~/Library/Application Support/Claude/claude_desktop_config.json`
- Windows: `%APPDATA%\Claude\claude_desktop_config.json`
- Linux: `~/.config/Claude/claude_desktop_config.json`

---

### ❌ แปลภาษาไม่ได้

**อาการ:** Error: `401 Unauthorized`

**วิธีแก้:**
1. เช็ค API Key ใน `.env`
2. เช็คว่ามีเครดิตเหลือ:
   - OpenAI: https://platform.openai.com/usage
   - Anthropic: https://console.anthropic.com/settings/billing
3. ลองสร้าง API Key ใหม่

---

### ❌ โพสต์ไป Twitter ไม่ได้

**อาการ:** Error: `403 Forbidden`

**วิธีแก้:**
1. เช็ค Bearer Token
2. เช็ค App Permissions (ต้องเป็น **Read and Write**)
3. สร้าง Token ใหม่
4. รอ 1-2 นาที แล้วลองใหม่

---

## ❓ FAQ

### Q1: ใช้ฟรีได้ไหม?

**โปรแกรม:** ฟรี 100%
**API Keys:**
- Twitter, Facebook, YouTube: ฟรี
- AI Translator: เสียเงินเล็กน้อย (~$0.15-0.25/1000 คำ)
- Gemini: ฟรี!

---

### Q2: ปลอดภัยไหม?

**ใช่!**
- ✅ Open Source - ตรวจสอบโค้ดได้
- ✅ API Keys เก็บในเครื่องคุณ
- ✅ ไม่ส่งข้อมูลไปไหน

---

### Q3: รองรับภาษาไทยไหม?

**รองรับ!** แปลจากจีนเป็นไทยได้

แก้ใน `pkg/processor/processor.go`:
```go
// เปลี่ยนจาก
targetLang := "en"

// เป็น
targetLang := "th"
```

---

### Q4: ต้องใช้กับ Claude Desktop เท่านั้นเหรอ?

**ไม่!** ใช้กับ AI ใดก็ได้ที่รองรับ MCP:
- Claude Desktop
- Cursor
- AI อื่นๆ ที่รองรับ MCP

---

### Q5: โพสต์ได้กี่ครั้งต่อวัน?

**ไม่มีข้อจำกัดจากโปรแกรม**

แพลตฟอร์มมีข้อจำกัด:
- Twitter: ~2,400 tweets/วัน
- Facebook: ไม่จำกัด
- TikTok: 3-5 วิดีโอ/วัน (แนะนำ)
- YouTube: ไม่จำกัด

**แนะนำ:** อย่าโพสต์เยอะเกินไป อาจโดน spam

---

### Q6: ต้องเปิดโปรแกรมทิ้งไว้ตลอดไหม?

**ขึ้นอยู่กับการใช้งาน:**
- โพสต์แบบ manual: เปิดเฉพาะตอนใช้
- ใช้ n8n automation: ต้องเปิดทิ้งไว้

**Tips:** รันบน VPS จะดีกว่า (ทำงาน 24/7)

---

## 🔗 Links เพิ่มเติม

- 🐛 **Report Bug:** https://github.com/huge8888/xiao-world/issues
- 🤖 **n8n Integration:** https://github.com/huge8888/n8n-xiao-world
- 📚 **MCP Protocol:** https://modelcontextprotocol.io

---

## 🙏 ขอบคุณ

โปรเจคนี้พัฒนาต่อยอดจาก:
- [xiaohongshu-mcp](https://github.com/xpzouying/xiaohongshu-mcp) โดย @xpzouying
- ชุมชน Go และ MCP Protocol

---

## 📄 License

MIT License - ใช้ฟรี แก้ไขได้ แจกจ่ายได้

---

<div align="center">

## 🌟 ให้ดาวถ้าชอบนะครับ!

**สนุกกับการเผยแพร่เนื้อหาไปทั่วโลก! 🌍✨**

Made with ❤️ in Thailand 🇹🇭

</div>
